const POSTFIX = ".sacastats.flex.bootstrap.table";

//Main Events
export var initializedEvent = "initialized" + POSTFIX;
export var formatsUpdatedEvent = "formats-updated" + POSTFIX;

//Filter Events
export var filteredEvent = "filtered" + POSTFIX;
